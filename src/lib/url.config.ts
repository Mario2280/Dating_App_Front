export enum UrlConfig {
  PROFILE = "/profile",
  MEDIA = "/media",
  PAYMENT = "/payment",
  MATCHES = "/matches",
  MESSAGES = "/messages",
  WALLET = "/wallet", // Add wallet endpoint
  NOTIFICATIONS = "/notifications",
}
